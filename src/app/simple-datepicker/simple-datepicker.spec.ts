import { Day } from './simple-datepicker';

describe('SimpleDatepickerClass', () => {
  it('should create an instance', () => {
    expect(new Day({
      date: new Date(),
      enable: true
    })).toBeTruthy();
  });

  it('should create an instance', () => {
    expect(new Day({
      date: new Date(),
      enable: true
    })).toBeTruthy();
  });
});
