import { SimpleDatepickerDirective } from './simple-datepicker.directive';

describe('SimpleDatepickerDirective', () => {
  it('should create an instance', () => {
    const directive = new SimpleDatepickerDirective();
    expect(directive).toBeTruthy();
  });
});
